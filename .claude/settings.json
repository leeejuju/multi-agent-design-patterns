{
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); cwd=$(echo \"$input\" | jq -r '.cwd'); branch=$(cd \"$cwd\" 2>/dev/null && git branch --show-current 2>/dev/null || echo ''); dirname=$(basename \"$cwd\"); if [ -n \"$branch\" ]; then printf '\\w %s (%s)' \"$dirname\" \"$branch\"; else printf '\\w %s' \"$dirname\"; fi"
  }
}
